# v136 구현 범위와 검증 상태

코드·로컬 실행 검증 완료. 운영 배포, 운영 DB 마이그레이션, 실데이터 검증 모델 활성화는 아직 하지 않았습니다.

## 구현한 AI 본체

- 사전학습 모델: `sentence-transformers/all-MiniLM-L6-v2`, 고정 revision `1110a243fdf4706b3f48f1d95db1a4f5529b4d41`.
- PC의 PyTorch에서 마지막 두 Transformer 블록을 실제 미세조정합니다. 단순 프롬프트 또는 규칙 이름을 AI로 바꾼 방식이 아닙니다.
- 정규화된 옵션 문자열과 수치 특징을 함께 입력하고, 가치 4분류·검수 2분류를 분리해 학습합니다. 원시 판매글을 명령으로 실행하지 않습니다.
- 수치 입력 MLP로 지식 증류하고, 서버가 그 **최종 MLP**를 다시 검증합니다. 웹 추론에는 PC나 외부 AI API가 필요하지 않습니다.
- 데이터 동기화 → 새 데이터 학습 → 별도 검증 → 통과 모델 자동 적용을 PC 실행기가 반복합니다. 기본 15분 확인, 실제 학습 시도 최대 하루 1회, 한 번 최대 30분입니다. 시간 초과는 실패로 남기고 기존 모델을 유지합니다.
- 이전 학습 원본·라벨이 유지된 경우에만 체크포인트를 이어 학습합니다. 수정·삭제된 표본이 있으면 사전학습 모델부터 다시 시작합니다.

## 데이터와 통계 검증

- 원본 스냅샷 SHA-256·옵션 파싱·검수 주체를 확인합니다. 같은 매물·같은 옵션 문맥은 한 묶음으로 처리합니다. 상충하는 정답은 제외합니다.
- 가치 태그가 있는 사람 검수만 가치 정답입니다. 승인/폐기는 별도 검수 정답이며, 가격을 이유로 한 판정은 가격 없는 검수 모델에서 제외합니다.
- 라벨 없는 수집품 중 기존 엔진으로 표현 가능한 항목은 가중치 0.15의 **약한 초기학습 라벨**을 사용합니다. 약한 라벨과 자동 판정은 사람 정답이나 검증 정답이 되지 않습니다.
- 회차당 최신 유효 기록 최대 2,000개를 선택한 뒤 중복을 제거합니다. 원본 이력은 삭제하지 않습니다. 대용량·과거 전체 재학습은 별도 확장이 필요합니다.
- 매물/지문 묶음별 영구 해시 분할: 학습 70%, 조정 10%, 시험 20%의 기대 비율입니다. 작은 데이터의 실제 비율은 다릅니다. **시간순 홀드아웃은 아니며**, 시험은 최근 90일의 사람 판정만 사용합니다. 시험 라벨은 PC에 보내지 않습니다. 반복 시험셋 사용에 따른 선택 편향은 향후 새 기간 전향 검증으로 추가 점검해야 합니다.
- 가치 적용: 예측 가능한 독립 시험 최소 30건, 정답 2종 각 5건 이상, 정확도 80% 이상, 95% Wilson 하한 65% 이상, 학습 라벨 빈도 기준보다 Brier 오차 0.01 이상 개선.
- 부위·종류·직업·래더별로도 검증을 통과해야 합니다. 비교 가능한 항목에서는 보존된 기존 OCR 감정 엔진 점수를 4등급으로 투영한 기준보다 정확도가 낮으면 제외합니다. 이 비교는 수동 감정의 모든 설명·가중치까지 동일하다는 의미는 아닙니다.
- 자동 검수는 정확도 95%, 신뢰 하한 85%를 요구하고 승인/폐기 방향별 정밀도도 따로 확인합니다. 원본을 재조회한 뒤 매시간 최대 3건 처리합니다. 기존 통계 자동 검수는 별도로 유지됩니다.
- 현재 활성 모델과 공통으로 판단하는 시험 항목에서 오차가 악화되면 교체하지 않습니다. 두 감정기는 동일한 비공개 서버 추론을 사용합니다. 기존 수치 점수는 보존하고, 검증된 가치 분류만 결과 제목에 표시합니다.

## 보호 장치

- 모르는 옵션, 중복 정체성, 판독 충돌, 학습 범위 밖 수치·요구레벨, 미검증 아이템군에서는 기존 감정 유지.
- 검수 수정/삭제 후 진행 중 학습의 오래된 결과 승격 차단. 이미 승인/폐기된 원본이 수정/삭제되면 활성 모델을 해제하고 재검증 대기.
- 모델 유효기간 90일, 관리자 중지/복귀, 만료되는 작업 임대권, 전용 학습 토큰, 업로드 크기 제한.
- 입력 변경 후 늦게 도착한 응답은 폐기. 추론 서버 장애 시 기존 감정을 그대로 사용.
- 테스트 모델 배포 차단. 모델 가중치·표본·관리 정보는 공개 API로 반환하지 않음.
- Cloudflare 스킬의 로컬 검증·제한된 API·비공개 자격증명 원칙을 적용했습니다. 운영 배포 없이 로컬 D1/Workers와 배포 드라이런으로 확인했습니다.

## 한계와 남은 운영 단계

- 현재 연결 범위는 공통 수치 계약으로 표현되는 레어·매직·크래프트입니다. 유니크·세트·룬워드·베이스, 충전/발동/개별 기술 등 지원하지 않는 문맥은 기존 감정으로 남습니다. 기존 기능을 삭제한 것이 아닙니다.
- 현재 수집 원문에 요구레벨이 없어 학습하지 못한 경우, 요구레벨이 지정된 사용자 입력도 보류합니다. 근거 없이 일반/저레벨 듀얼 가치를 같게 보지 않습니다.
- 실제 체결가 예측, 거래소별 실시간 가격 환산, 패치/플랫폼/하드코어별 시세 보정, OCR 문자 인식 신경망 재학습은 이번 구현에 포함하지 않습니다. 래더/스탠과 일반 아이템 가치 분류를 다루는 모델입니다.
- 운영 DB 읽기 전용 집계(2026-09-22): 전체 223행, 승인/폐기 129행, 가치 태그 18행, 학습 허용+가치 태그 7행. 마지막 7행도 신규 파이프라인의 원본 검증·중복 제거 전 집계입니다.
- 따라서 **이미 정확도가 향상된 운영 모델이 완성됐다는 뜻은 아닙니다.** 배포와 PC 연결 후 실제 학습이 수행되고, 새 사람 검증 데이터로 적용 조건을 통과해야 합니다.
- 약한 라벨을 쓰면 모든 수집품을 사람이 직접 라벨링할 필요는 줄지만, 정확도 확인용 사람 정답을 없앨 수는 없습니다. 좋은 원본과 가치 라벨의 품질이 성능의 상한입니다.

## 재현 명령

`npm.cmd test` · `python tests/ai-runner.test.py` · `node tests/ai-browser.cjs` · `wrangler.cmd deploy --dry-run --keep-vars`

브라우저 테스트는 Playwright와 Edge가 필요합니다. 실제 파인튜닝 테스트는 테스트 전용 데이터·`--smoke`로 실행했으며, 실시장 정확도 수치로 보고하지 않습니다. 세부 결과는 `VERIFICATION.json`을 확인하세요.

참고: [Transformers 학습 문서](https://huggingface.co/docs/transformers/en/training), [모델 카드](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2), [D1 명령 문서](https://developers.cloudflare.com/d1/wrangler-commands/).
