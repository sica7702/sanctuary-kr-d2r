# Sanctuary v136 · PC 파인튜닝 자동 학습

v135를 보존한 새 배포본입니다. 기존 디자인·OCR 문자 인식·감정 점수 공식·득환 기준·아이템/빌드 DB는 유지했습니다.

이번에는 실제 사전학습 모델 미세조정, 사이트용 모델 변환, 서버 검증, 사진·수동 감정 연결, 자동 검수 경로와 PC 실행기를 함께 제공합니다. **운영에 배포하거나 실데이터 모델을 활성화한 상태는 아닙니다.**

## 1. 사이트 배포

ZIP을 **새 폴더**에 풀고 `DEPLOY.cmd`를 실행하세요. 확인 후 테스트 → 사전검사 → 운영 DB 백업 → 추가 테이블/안전 트리거 적용 → 사이트 배포 순서로 진행합니다. 관리자 비밀번호와 기존 설정은 유지합니다. Wrangler 로그인이 필요합니다.

명령어로 하려면 압축 푼 폴더의 PowerShell에서 실행하세요. 각 명령이 성공한 것을 확인하고 다음으로 넘어가세요.

```powershell
npm.cmd test
wrangler.cmd deploy --dry-run --keep-vars
$backupPath = 'backup-before-v136-' + (Get-Date -Format 'yyyyMMdd-HHmmss') + '.sql'
wrangler.cmd d1 export sanctuary-d2r-db --remote --output $backupPath
wrangler.cmd d1 execute sanctuary-d2r-db --remote --file .\migrations\0136_neural_learning.sql
wrangler.cmd deploy --keep-vars
```

백업 파일은 개인정보를 포함할 수 있으니 외부에 공유하지 마세요. 마이그레이션은 기존 기록을 삭제하지 않습니다. 배포하면 기존 시간별 작업에 새 데이터 동기화와, 검증 모델이 있을 때의 자동 검수가 추가됩니다.

## 2. PC에서 첫 한 번 설치

`SETUP-LEARNING.cmd`를 실행하세요. Python 3.11~3.13, Node.js가 필요하며 이 PC의 Codex 번들 Python이 있으면 먼저 사용합니다. 별도 `.learning-venv` 폴더에 설치하므로 기존 Python 환경을 변경하지 않습니다.

기본은 NVIDIA CUDA 12.8용 PyTorch입니다. 첫 설치는 라이브러리와 모델 다운로드 때문에 인터넷·저장 공간이 필요합니다. GPU 드라이버와의 호환은 실행 시 확인합니다. GPU를 사용하지 않으려면:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\learning\setup.ps1 -CPU
```

스크립트 실행 허용은 해당 프로세스에만 적용하며, PC의 전역 실행 정책은 바꾸지 않습니다.

## 3. 학습 시작

1. 관리자 → **통계 검증 → PC 파인튜닝 · 자동 학습 → PC 연결 키 발급**.
2. `START-LEARNING.cmd` 실행 → 사이트 주소 → 발급받은 키 입력.
3. 학습 창을 켜둡니다. 데이터를 가져와 학습·검증하고, 통과한 모델만 서버에 자동 적용합니다. 수동 파일 업로드는 필요 없습니다.

키는 전용 학습 권한이며 입력 화면에서 숨겨집니다. 키를 분실했거나 노출했다면 새로 발급하세요. Windows 자동 시작 등록은 하지 않았습니다.

## PC를 언제 켜야 하나요?

**새 학습을 돌릴 때만 필요합니다.** PC/실행기를 끄면 다음 학습은 멈추지만, 이미 검증·배포된 모델은 사이트 서버에서 계속 실행합니다. 단, 90일 만료·관리자 중지·검수 정정으로 해제되면 기존 감정으로 복귀합니다.

기본 확인 간격은 15분, 실제 학습 시도는 하루 최대 1회, 1회 학습 상한은 30분입니다. 실제 소요 시간은 데이터와 PC에 따라 다르며 설치·다운로드 시간은 별도입니다. 오류/중단 시 기존 감정을 유지합니다.

외부 유료 AI API나 클라우드 GPU는 사용하지 않습니다. PC 전력과 기존 Cloudflare Workers/D1 사용량은 발생하므로 무료 한도 내 이용을 무조건 보장하는 것은 아닙니다.

## 배포 즉시 전부 자동 판정되나요?

아닙니다. 코드는 준비됐지만 실데이터 검증을 통과한 모델은 아직 없습니다. 2026-09-22 운영 DB 집계상 학습 허용·가치 태그가 있는 행은 7개이고, 원본 검증과 중복 제거를 거치면 더 줄 수 있습니다.

라벨 없는 수집 데이터에도 기존 기준의 약한 초기학습을 적용하므로 모든 품목을 일일이 라벨링할 필요는 줄어듭니다. 다만 가치 등급을 선택한 사람 검수는 정확도 확인을 위해 계속 필요합니다. 승인은 ‘높은 가치’, 폐기는 ‘낮은 가치’라고 임의로 간주하지 않습니다.

학습은 독립 학습품 8개 이상부터 시도하지만, 사이트 적용에는 **아이템군별 독립 사람 시험 표본 최소 30개와 정확도·신뢰도 조건**이 따로 있습니다. 총 승인 수만으로 시작 시점을 정하지 않습니다. 자동 승인/폐기는 더 엄격하게 검증합니다.

현재 학습은 레어·매직·크래프트 중 옵션·상황을 정확히 표현할 수 있는 범위입니다. 미지원/불확실한 입력과 유니크·세트·룬워드·베이스는 기존 기능을 유지합니다. 실제 거래 가격 예측이나 OCR 글자 인식 모델을 재학습한 것은 아닙니다.

## 중지·복귀

관리자의 **학습·모델 사용 중지**는 새 학습과 새 모델 추론을 중지합니다. **기존 감정으로 복귀**는 활성 모델을 해제하고 진행 중인 학습의 승격을 취소합니다. 원본 기록은 삭제하지 않습니다. 이전 통계 자동 검수까지 전부 멈추려면 기존 Worker 환경 변수 `AUTO_REVIEW_DISABLE=true`도 적용하세요.

문제가 있으면 DB 백업을 성급하게 덮어쓰지 말고 먼저 모델만 중지하세요. 사이트 코드 자체를 되돌릴 때는 보존한 v135 폴더에서 다시 배포할 수 있습니다. 새 테이블은 그대로 둬도 됩니다.

검증 내역: `VERIFICATION.json` · 상세 구조/한계: `LEARNING-IMPLEMENTATION.md`.
