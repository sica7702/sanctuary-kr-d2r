{
  "name": "sanctuary-kr-d2r-ui-v136",
  "version": "1.36.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --keep-vars",
    "check": "wrangler deploy --dry-run --keep-vars",
    "test": "node --test tests/*.test.mjs"
  }
}
