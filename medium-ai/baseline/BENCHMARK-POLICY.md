# Baseline benchmark policy

This first benchmark is intentionally read-only. It records production-file syntax checks and extension smoke-test latency before any provider or Python engine is connected.

It is not a model-quality benchmark. A model-quality baseline will be added only after a frozen human-reviewed holdout set and a provider are available.
